<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sites BNF</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap');
    body {
      margin:0;
      font-family: 'Inter', sans-serif;
      background: radial-gradient(circle at 10% 10%, #020024, #0b0f2b);
      color: #dff;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    header {
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: rgba(0,0,0,0.3);
      box-shadow: 0 0 20px #7c3aed;
    }
    h1 { font-size:2rem; color:#7c3aed; }
    p { font-size:1.1rem; }
    .hero {
      flex:1;
      display:flex;
      flex-direction:column;
      justify-content:center;
      align-items:center;
      text-align:center;
      padding:2rem;
      background: radial-gradient(circle, rgba(124,58,237,0.2), rgba(2,0,36,0.7));
      transition: all 0.5s ease;
    }
    .hero h2 { color:#00ffff; font-size:2rem; margin-bottom:1rem; text-shadow:0 0 10px #0ff; }
    .hero p { color:#dff; text-shadow:0 0 5px #fff; }
    .button-whatsapp {
      margin-top:1.5rem;
      padding:0.8rem 1.5rem;
      background:#25D366;
      color:#fff;
      font-weight:bold;
      border:none;
      border-radius:50px;
      cursor:pointer;
      text-decoration:none;
      box-shadow:0 0 15px #25D366;
      transition: all 0.3s ease;
    }
    .button-whatsapp:hover {
      transform: scale(1.05);
      box-shadow:0 0 25px #25D366;
    }
    footer {
      padding:1rem 2rem;
      text-align:center;
      background: rgba(0,0,0,0.3);
      box-shadow: 0 -0 20px #7c3aed;
    }
  </style>
</head>
<body>
  <header>
    <h1>Sites BNF</h1>
    <nav><a class="button-whatsapp" href="https://wa.me/5519982965458" target="_blank">Fale no WhatsApp</a></nav>
  </header>

  <section class="hero">
    <h2>Criação de sites profissionais e personalizados</h2>
    <p>Transformamos suas ideias em sites modernos com efeitos futuristas e interativos.</p>
  </section>

  <footer>
    &copy; 2025 Sites BNF • Contato: <a href="https://wa.me/5519982965458" style="color:#0ff;">WhatsApp</a>
  </footer>
</body>
</html># Sample workflow for building and deploying a Jekyll site to GitHub Pages
name: Deploy Jekyll with GitHub Pages dependencies preinstalled

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
